extends global_layout.pug

append head
    title= __('Categories')

block content
    h2.archive-label.new!= __('Categories')
    ul.index-ul.tags.new
        each category in site.categories
            li
                a.--smooth(href=url_for('/categories/'+category.key+'/0/'))= category.key